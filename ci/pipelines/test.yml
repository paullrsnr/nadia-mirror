############################
# BACKEND PYTHON (pytest)
############################
test:backend:
  stage: test
  image: python:3.12-slim

  before_script:
    - cd backend
    - pip install -r requirements.txt

  script:
    - cd backend
    - pytest tests/ -v --tb=short

  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - backend/**/*
    - if: '$CI_PIPELINE_SOURCE == "push"'
      changes:
        - backend/**/*

  coverage: '/TOTAL.*\s+(\d+%)$/'

############################
# DESKTOP TYPESCRIPT (Jest/Vitest)
############################
# test:desktop:
#   stage: test
#   image: node:22-bullseye
#
#   before_script:
#     - cd desktop
#     - npm ci
#
#   script:
#     - cd desktop
#     - npm run test
#
#   rules:
#     - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
#       changes:
#         - desktop/**/*
#     - if: '$CI_PIPELINE_SOURCE == "push"'
#       changes:
#         - desktop/**/*
